Simple server-uptime email alerts.

Example crontab entry for minutely checks with logs to pingr.log:
  * * * * * sh /root/pingr/pingr.sh >> /root/pingr/pingr.log 2>&1

TODO: Convert to Ruby and add Twilio text alerts.